

const Error = ({error}) => {
    return (
        <div className="alert alert-danger" role="alert my-5 text-center">
            {error}
        </div>
    );
}

export default Error;
        
          